## JellyRip v1.0.18 Release Notes (UNSTABLE)

Release channel: unstable pre-release.
Close to stable, but still lightly tested.

### Download
- Direct download: https://github.com/unexpear/JellyRip/releases/download/v1.0.18/JellyRip.exe
- Installer: https://github.com/unexpear/JellyRip/releases/download/v1.0.18/JellyRipInstaller.exe
- Release page: https://github.com/unexpear/JellyRip/releases/tag/v1.0.18
- All releases: https://github.com/unexpear/JellyRip/releases

### What's New in 1.0.18

Transcode path recovery
- Transcode prep now falls back to the valid detected FFmpeg or HandBrakeCLI path when a saved configured path no longer resolves.
- That restores the expected upgrade behavior where bundled binaries and known install locations still work without forcing an immediate settings repair.

Manual movie edition handling
- Manual movie-disc runs now carry the selected edition into controller state and destination folder naming.
- Alternate cuts no longer collapse into the base movie folder during the manual flow.

Save and update robustness
- Settings saves now avoid partial config commits when the paired expert-profile write fails.
- The signed-update block path now shows the intended GUI error instead of throwing a callback arity exception.
- Verification fallback retries no longer announce a rejected transcode as completed before the retry starts.

Release hygiene
- Release metadata now aligns on the 1.0.18 line across the app, installer, docs, tester worksheet, and release notes.
- MAIN builds continue to stage `JellyRip.exe`, `JellyRipInstaller.exe`, bundled FFmpeg binaries, and notice files under `dist/main`.
