"""RETIRED in Phase 3h (2026-05-04).

This file pinned the tkinter button color constant
``gui.setup_wizard._ACCENT_BUTTON_BG`` and its WCAG contrast ratio.
After tkinter retirement the constant no longer exists; per-theme
WCAG contrast is now pinned in ``tests/test_pyside6_themes.py`` (98
parametrized tests across 6 themes).

The file is left as an empty module because the sandbox cannot
delete files; it collects zero tests.
"""
