"""RETIRED in Phase 3h (2026-05-04).

This file pinned tkinter-side helpers in ``gui/main_window.py`` that
were retired in Phase 3h. Equivalent coverage now lives in
``tests/test_pyside6_formatters.py`` (per the Phase 3g audit at
``docs/handoffs/phase-3g-test-audit.md``).

The file is left as an empty module because the sandbox cannot
delete files; it collects zero tests.
"""
