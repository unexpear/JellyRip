"""RETIRED in Phase 3h (2026-05-04).

This file pinned tkinter widget color constants in
``gui/setup_wizard.py`` for the EXTRA-label and LibreDrive UX fixes.
After tkinter retirement those constants no longer exist. Equivalent
UX coverage is in the per-step pytest-qt wizard tests
(``tests/test_pyside6_setup_wizard_*.py``) plus the WCAG contrast
suite in ``tests/test_pyside6_themes.py``.

The file is left as an empty module because the sandbox cannot
delete files; it collects zero tests.
"""
